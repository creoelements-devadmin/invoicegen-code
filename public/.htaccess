<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /invoicegen/
  
  # SPA Fallback
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /invoicegen/index.html [L]
</IfModule>
